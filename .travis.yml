language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-node-cs' ]; then cd .. && eval "mv $currentfolder generator-node-cs" && cd generator-node-cs; fi

